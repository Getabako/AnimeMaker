// Codex に渡す指示プロンプト。画像生成は Codex が自身のツールで実行する。
// 外部 API キー (OPENAI_API_KEY 等) は使用禁止。

const COMMON_RULES = `# 🚨 ツール使用ルール（絶対）

- 外部 API キー (OPENAI_API_KEY, GEMINI_API_KEY 等) は **使用禁止**。.env も作らない。
- 画像生成は **あなた (Codex) 自身が持っている画像生成ツール** で行う。
  外部の HTTP / curl で画像生成 API を叩かない。
- 参照画像は cwd 内に \`reference.png\` として既に置かれている。これを必ず参照する。
- 出力ファイルは cwd 直下に書く。サブディレクトリを切らない。
- 生成が終わったら **必ず** \`manifest.json\` を書き出して終了。`;

const CHARACTER_CONSISTENCY = `# キャラクター一貫性（最重要）

各画像で **絶対に守ること**:
- 参照画像のキャラクター ID・シルエット・体型・顔・目・配色・衣装・装飾・線画スタイル・全体デザインを厳密に維持
- 線画の太さ・ピクセルの粗さ・解像感（ドット絵ならドット絵のまま）・配色・絵柄を参照と一致させる
- ただし「写し取る」「切り抜く」「貼り付ける」「コピーする」発想は **絶対禁止**。
  参照画像はキャラデザの見本として頭で理解するだけ。実際の描画は毎回まっさらなキャンバスにゼロから新規描画する。
- キャラクターをリデザインしない
- 別キャラを描かない / 追加キャラを描かない
- 腕の本数を変えない / 装飾を変えない / 配色を変えない
- 顔・目の形・色を変えない / 顔をぼやかさない / 髪型の輪郭を崩さない

## 頭身比・体型の厳守（最重要）

過去の失敗例：参照画像が 2 頭身のちびキャラなのに、生成画像では 4〜5 頭身の
リアル体型に伸びてしまい、別キャラのようになった。これは絶対 NG。

- 参照画像の **頭身比（head-to-body ratio）を必ず維持** する。
  2 頭身のちびキャラなら 2 頭身、6 頭身のリアル体型なら 6 頭身。勝手に伸ばすな。
- 参照画像をよく観察し、頭の縦サイズを 1 単位として、胴体・腕・脚の長さがそれぞれ
  頭何個分なのかを把握してから描き始める。生成画像でもこの比率を完全に再現する。
- ちびキャラ・デフォルメキャラを **リアル頭身に翻訳しない**。デフォルメ感を維持。
- リアル頭身キャラを **ちびに縮めない**。
- 体の各パーツの相対比率（頭：胴：腕：脚）を参照と一致させる。
- 「動きを大きく見せたい」という理由で手足を引き伸ばすのも NG。`;

const SCENE_RULES = `# 画面ルール

- キャラクター 1 体のみ。全身が画面内に収まる。
- 背景は **完全な単色** (フラット塗りつぶし)。グラデ・模様・テクスチャ禁止。
- 影・地面・エフェクト・煙・魔法陣・武器追加・小物追加・テキスト・ロゴ・風景は一切描かない。
- 背景色は **キャラクターに含まれない色** を選ぶ。原則 \`#FF00FF\` (マゼンタ) を使う。
  ただしキャラがピンク系の場合は \`#00FF00\` か \`#00FFFF\` を選ぶ。
- ゲームスプライト用途。輪郭が背景と分離して切り抜きやすい状態に保つ。

## キャンバスサイズの選び方（最重要）

過去の失敗例：腕を伸ばすポーズが指定サイズに入りきらず、キャラ全体を縮めて
無理やり押し込んだ結果、頭身比・体格・パーツサイズが参照と乖離した。これは絶対 NG。

正しい順序:
1. まずキャラを **参照画像と完全に同じ縦横比・同じパーツサイズ** で描くと決める。
   キャラのスケールは参照に固定。これは絶対に変えない。
2. その上で、指定されたポーズ／動作が **指定キャンバスサイズに収まるか** を判定する。
3. 収まる → 指定キャンバスサイズで出力。キャラは参照と同じ大きさで中心に配置。余白は背景色。
4. 収まらない → **キャンバスを拡張**（縦・横・両方のうち必要な分だけ）して、ポーズが余白付きで
   入る大きさにする。キャラのスケールは変えない。あくまでキャンバスを広げて対応する。

つまり「**キャラを縮めて画面に押し込む**」のではなく「**画面の方をキャラに合わせて広げる**」。
ゲームで使う時に基準点（足元中央）が同じなら、画面サイズが多少違っても問題ない。`;

export type PoseParams = {
  posePrompt: string;
  backgroundColor?: string;
  size?: string; // "1024x1024" | "1024x1536" | "1536x1024"
};

export function buildPoseCodexPrompt(p: PoseParams): string {
  const bg = p.backgroundColor || "auto (キャラに含まれない色を選ぶ。原則 #FF00FF)";
  const size = p.size || "1024x1024";
  return `# あなたへの作業指示 — ポーズ作成モード

cwd の \`reference.png\` を厳密な参照キャラクター画像として扱い、
**同一キャラクターのポーズ違い画像 1 枚** を生成してください。

${COMMON_RULES}

${CHARACTER_CONSISTENCY}

${SCENE_RULES}

# ポーズ指示
ユーザー入力: 「${p.posePrompt}」

ポーズ**だけ**を変える。デザインは一切変えない。

# 出力
- ファイル名: \`pose.png\`
- 推奨キャンバスサイズ: ${size}（最低保証サイズ。**ポーズが収まらない場合は拡張可**）
- キャラのパーツサイズ・頭身比は参照画像と完全一致させる（縮めて押し込むの禁止）
- ポーズが ${size} に収まる → ${size} で出力
- 収まらない → 必要分だけキャンバスを拡張して出力（例: 1024x1024 → 1280x1024 / 1024x1280 / 1280x1280 等）
- 背景色: ${bg} (完全な単色フラット塗り)

# 完了処理
生成後、cwd 直下に \`manifest.json\` を書き出してください。形式:
\`\`\`json
{
  "mode": "pose",
  "image": "pose.png",
  "backgroundColor": "実際に使った #RRGGBB",
  "size": "実際に出力したサイズ WxH",
  "requestedSize": "${size}",
  "expanded": true/false,
  "userPrompt": "${escape(p.posePrompt)}"
}
\`\`\`
`;
}

export type AnimationParams = {
  motionPrompt: string;
  framesBefore: number; // 既定 3
  framesAfter: number;  // 既定 3
  motionStrength: "weak" | "medium" | "strong";
  loop?: boolean; // ループアニメ (歩行/待機など)
  backgroundColor?: string;
  size?: string;
};

export function buildAnimationCodexPrompt(p: AnimationParams): string {
  const bg = p.backgroundColor || "auto (キャラに含まれない色。原則 #FF00FF)";
  const size = p.size || "1024x1024";

  const loop = !!p.loop;
  const totalFrames = p.framesBefore + p.framesAfter + 1;
  const frameOrder: number[] = [];
  for (let i = -p.framesBefore; i <= p.framesAfter; i++) frameOrder.push(i);

  const frames: string[] = frameOrder.map((i, idx) => {
    const role = loop
      ? loopFrameRole(idx, totalFrames, frameOrder)
      : frameRole(i);
    return `- ${frameFileName(i)} : ${role}`;
  });

  const headerLine = loop
    ? `**合計 ${totalFrames} 枚のループアニメ用キャラクター画像** を生成してください。
最後のフレームを過ぎたら最初のフレームに戻って自然に繋がる、シームレスなループを作ります。
（歩行アニメ・待機アニメなどの用途。frame_000 は「基準姿勢」ではなく、ループ中の 1 つの位相）`
    : `**前後 ${p.framesBefore} フレームずつ、合計 ${totalFrames} 枚** の
コマ送りアニメ用キャラクター画像を生成してください。
順番につなげると自然な連続動作に見えるようにします。
（攻撃・ジャンプなど一方向の単発アクション用途。frame_000 が動作の基準姿勢）`;

  return `# あなたへの作業指示 — アニメーション作成モード

cwd の \`reference.png\` を厳密な参照キャラクター画像として扱い、
${headerLine}

${COMMON_RULES}

${CHARACTER_CONSISTENCY}

${SCENE_RULES}

# 動作指示
ユーザー入力: 「${p.motionPrompt}」
動きの強さ: **${p.motionStrength}** (${strengthGuide(p.motionStrength)})

# フレーム別の役割
中心フレーム \`frame_000.png\` を参照画像に最も近い基準姿勢とし、
前後フレームは以下の役割でそれぞれ生成すること:

${frames.join("\n")}

# 🚨 サイズ・スケール・カメラ固定（最重要・絶対）

過去の失敗例：あるフレームだけキャラが拡大されたり縮んだりして、サイズが揃わない。
ゲーム内でアニメを切り替えるとキャラが小さく見えてしまう。これは絶対に防ぐこと。

- 全フレームで **キャラクターの画面占有率を完全に同一** に保つ。
- 頭の上端の Y 座標、足元（裾の最下端）の Y 座標、体の中心の X 座標を **全フレームで揃える**。
- カメラのズーム・パン・回転は禁止。常に正面・同一画角・同一距離。
- キャンバスの解像度は **全フレーム同一**。アスペクト比も固定。
- 体格・身長・横幅は変えない。アップやロングにしない。

## キャンバスサイズの決め方（最重要）

過去の失敗例：手を伸ばすフレームが指定サイズに入りきらず、そのフレームだけキャラを縮めて
押し込んだ結果、ゲームでアニメを再生したらキャラが伸び縮みして見えた。これは絶対 NG。

正しい手順:
1. キャラのパーツサイズ・頭身比は **参照画像と完全一致** で固定。これは変えない。
2. これから描く全フレームの中で、**一番大きく手足を伸ばすフレーム**（例: フォロースルーの +3）を
   先にイメージし、それが収まるキャンバスサイズを判定する。
3. 指定サイズ ${size} に収まる → ${size} で全フレーム出力。
4. 収まらない → 必要分だけキャンバスを拡張（例: 1024x1024 → 1280x1024 等）し、**全フレーム同じ拡張後サイズで出力**。
   一部フレームだけサイズが違うのは厳禁。
5. 拡張後の足元基準点（X中央・Y足元）は全フレーム揃える。

つまり「キャラを縮めて画面に押し込む」のではなく「画面の方をキャラに合わせて広げる」。

## パーツ単位のサイズ保持（特に重要）

過去の失敗例：腕を動かすために体ごと一回り小さく描き直してしまい、ゲーム内で
アニメを切り替えた瞬間にキャラが縮んで見える事故が起きた。これは絶対 NG。

各パーツ（頭・顔・胴体・腰・腕の各セグメント・脚の各セグメント・装飾・武器など）の
**ピクセル上の長さ・幅・面積を、参照画像とすべてのフレームで完全に一致させる**こと。

- 頭の縦横サイズ・顔のパーツ間隔は参照と完全一致。
- 胴体の幅・縦の長さは参照と完全一致。
- 上腕・前腕・大腿・下腿それぞれの長さは参照と完全一致（角度だけ変える）。
- 装飾・髪・服の各要素の大きさも参照と一致。
- 動かす部位は **角度と位置だけ変える。長さや太さを変えてはいけない。**
  「腕を伸ばしたら腕が長くなった」「振りかぶったら拳が小さくなった」は全部 NG。
- パースで奥行きを誤魔化してパーツを縮めるのも NG。常にフラットな正面ビュー。
- 全フレームで頭部矩形の幅・高さがピクセル単位で同一かを描画後に必ず確認すること。

# 🚨 連続動作の必須要件（最重要・絶対）

過去の失敗例：
- 隣接フレームがほぼ同じで止まって見える
- 飛んでいて繋がらない
- **参照画像をそのまま歪めたり拡大縮小しているだけで、関節・手先・足先の位置が全く変わっていない（最頻出の失敗）**

特に最後のパターンは絶対に防ぐこと。
**「同じポーズの画像をブニョブニョ膨らませて差分を作る」ことは禁止。これはアニメーションではない。**

必ず守ること:

- ユーザーが指定した動作部位（腕・脚・武器・髪 等）について、
  **各フレームで関節の角度と手先（または末端）の絶対座標が明確に異なっていること**。
  例: 腕を振るなら、frame_-3 → +3 で手先の位置が画面上で確実に別の場所にある。
  「同じ腕の位置のまま少し歪んだ」は不可。
- 各フレームを描き始める前に、その部位がどの角度・どの位置にあるかを内部で決めること。
  参照画像をそのまま少し歪めただけ、は NG。**動かす部位は新しい位置でゼロから描く。**

# 🚨 描画手法の絶対ルール（最重要）

## 禁止される手法（厳守）
以下の手法を 1 つでも使ったら失敗とみなす。やるな:
- 参照画像からキャラのパーツを **切り抜いて新しい場所に貼り付ける**
- 参照画像 / 直前フレームに腕や脚を **重ねて描き足す**
- クローンスタンプ・コピー＆ペースト・レイヤー合成・スタンプブラシ
- 1 枚の画像内で同じパーツ（腕など）を **複数のポーズで重ねて描く**
- ある部位を **半透明やゴースト化して動きを表現する**
- 参照画像をそのまま少し変形・歪曲・拡大縮小して新フレームを作る

上記の手法は **どれもまともなアニメ画像を作れない**。やめろ。

## 必ずやる手法
- 各フレームは **空のキャンバスに最初の 1 線から最後まで、1 枚絵としてゼロから描き上げる**。
  参照画像はキャラデザを目で見て理解するための見本にすぎない。
- そのフレームの瞬間に存在する身体・服・装飾「だけ」を、その位置に普通の絵として描く。
  動かす部位はその瞬間の 1 ポーズだけ描く。それ以前のポーズの残骸は存在しない。
- 描き終わった画面には:
  - キャラのシルエット内部 = キャラ本体だけ
  - 背景部分 = 指定背景色 1 色だけ（線も影も破片もゴーストも一切無し）

# 🚨 キャラ描画と動作描画の分離

- 顔・目・口・髪型・服のデザイン・配色・線画スタイル・解像感は、
  参照画像と同じ絵柄に **ゼロから描き直して** 揃える（パーツを移植するのではない）。
- 動作部位（腕・脚など）は その瞬間のポーズだけを **ゼロから新規に描く**。
  古いポーズの腕は存在しない。新しい位置にだけ腕が描かれている状態。
- 各フレームは **直前フレームの中間補間として物理的に成立する** 動きでなければならない。
  frame_-3 → -2 → -1 → 000 → +1 → +2 → +3 を順番に再生すれば、1 つの滑らかなアクションになること。
- **隣接フレーム間に必ず視認できる位置差分を入れる**（変形だけの「ほぼ同じ画像」は NG）。
- 同時に **隣接フレームから離れすぎてもいけない**（コマ抜けに見えると NG）。
- 体全体が連動して動く：肩・腰・胴体・首・頭・腕・指・髪・装飾・裾・足元すべてに段階的な追従動作を入れる。
  ただし**ユーザーが指定した主動作部位は他より明確に大きく動く**（脇役の追従より目立つこと）。
- 動作の前半（-3 → 000）と後半（000 → +3）で動きの向きと加速度が一貫していること。
${loop ? `
# 🚨 ループアニメ専用ルール（最重要・絶対）

このアニメは **シームレスにループ再生される**。最後のフレーム ${frameFileName(p.framesAfter)} を再生した次のコマで
最初のフレーム ${frameFileName(-p.framesBefore)} に戻る。視聴者にはどこがループ境目か分からないようにすること。

- 最後のフレーム ${frameFileName(p.framesAfter)} と最初のフレーム ${frameFileName(-p.framesBefore)} の差分は、
  他の隣接フレーム同士の差分と**同じくらい滑らか**でなければならない。
  （つまり「最初と最後がほぼ同じ」ではダメ。「最後の次は最初」として補間が成立する位置関係にする。）
- frame_000 を「動作の基準姿勢（タメ前のニュートラル）」として扱わない。これはループの 1 位相にすぎない。
- 動作のサイクルを全 ${totalFrames} フレームに均等配分する。例えば歩行なら 1 歩分または 2 歩分の周期を ${totalFrames} 等分する。
- 始点と終点で体の重心・腕脚位置・髪の位置が同位相になるようにループを設計する。
` : ""}

# 出力
- ファイル名は上記のとおり連番で cwd 直下に書く。
- 全フレーム同一サイズ。指定 ${size} で全ポーズが収まればそのサイズ。
  収まらないポーズがあれば必要分だけ拡張した同一サイズで全フレームを揃える。
- 全フレーム同一の単色背景: ${bg}（厳密に同一色）

# 🔁 完了前のセルフチェック（必ず実行）

全フレームを書き出したあと、**自分で生成画像を順番に並べて見比べ**、以下を確認すること:

1. キャラクターのサイズ・位置・画面占有率がすべてのフレームで揃っているか？
   特に **頭の縦横ピクセル数・胴体の幅と高さ・腕脚の各セグメント長** が参照と全フレームで一致しているか？
   → 1 つでも縮小/拡大があれば、ゲームでアニメ切替時にキャラが小さく見える。必ず描き直し。
1b. **参照画像の頭身比と完全に一致しているか？**
   参照が 2 頭身のちびキャラなら全フレームも 2 頭身。参照が 6 頭身ならフレームも 6 頭身。
   → 胴体や手足を伸ばしてリアル頭身に変えてしまったら、別キャラに見える。必ず描き直し。
2. **顔・目・口・髪型・服の絵柄は参照画像と全フレーム同一に見えるか？**
   → 顔がぼやけたり髪型が崩れていたら作り直し。
3. **背景は完全な単色か？古いポーズの腕の残像・破片・切れ端・クローン跡が背景に残っていないか？**
   → 背景にキャラ以外の線・色・残像が 1 ピクセルでもあれば作り直し。切り貼り跡禁止。
4. **動作部位（腕等）の手先の位置が、各フレームで明確に別の場所にあるか？**
   → 全フレームで同じ位置にあるなら作り直し。「歪んでるだけ」は失敗。
5. **同じフレーム内に、同じ部位（例：腕）が複数のポーズで重なって描かれていないか？**
   → ゴースト・残像・二重露光のような状態は失敗。1 ポーズだけを描く。
6. 隣接フレーム同士が「同じポーズを変形しただけ」になっていないか？
7. -3 → +3 を順に並べたとき、一連の動作として滑らかに繋がるか？
8. 主動作以外の部位は脇役レベルの追従に留まっているか？
${loop ? `9. **最後のフレーム → 最初のフレームの繋がりが、他の隣接フレーム同士と同じくらい滑らかか？**
   → ループ境目が分かるようでは失敗。位相を計算し直して描き直す。` : ""}

**1 つでも NG があれば必ず該当フレームを破棄して描き直すこと。リリース品質に達しないものを提出しない。**

問題があるフレームは **必ず生成し直してから** \`manifest.json\` を書いて終了すること。
妥協して提出しないこと。

# 完了処理
全フレーム生成後、cwd 直下に \`manifest.json\` を書き出してください。形式:
\`\`\`json
{
  "mode": "animation",
  "frames": [
    { "index": -3, "file": "frame_-3.png" },
    { "index": -2, "file": "frame_-2.png" },
    { "index": -1, "file": "frame_-1.png" },
    { "index": 0,  "file": "frame_000.png" },
    { "index": 1,  "file": "frame_+1.png" },
    { "index": 2,  "file": "frame_+2.png" },
    { "index": 3,  "file": "frame_+3.png" }
  ],
  "backgroundColor": "実際に使った #RRGGBB",
  "size": "実際に出力したサイズ WxH（全フレーム共通）",
  "requestedSize": "${size}",
  "expanded": true/false,
  "motionStrength": "${p.motionStrength}",
  "loop": ${loop ? "true" : "false"},
  "userPrompt": "${escape(p.motionPrompt)}"
}
\`\`\`
(framesBefore=${p.framesBefore}, framesAfter=${p.framesAfter} の実数に合わせて配列を作る)
`;
}

export function frameFileName(i: number): string {
  if (i === 0) return "frame_000.png";
  if (i < 0) return `frame_${i}.png`;
  return `frame_+${i}.png`;
}

function loopFrameRole(idx: number, total: number, order: number[]): string {
  const phase = Math.round((idx / total) * 100);
  const nextIdx = (idx + 1) % total;
  const prevIdx = (idx - 1 + total) % total;
  const nextName = frameFileName(order[nextIdx]);
  const prevName = frameFileName(order[prevIdx]);
  return `ループサイクルの位相 ${phase}% (${idx + 1}/${total} 枚目)。動作サイクル全体を ${total} 等分した時刻の姿勢を描く。前 (${prevName}) と次 (${nextName}) の中間に物理的に位置する姿勢。サイズ・足元位置・カメラは全フレーム共通。`;
}

function frameRole(i: number): string {
  if (i === 0) return "基準姿勢。参照画像に最も近い状態。微調整のみ。サイズ・位置は参照と完全一致。";
  if (i < 0) {
    const n = -i;
    return `動作開始 ${n} ステップ前の準備姿勢（タメ）。動作方向とは逆側に主動作部位（腕など）の関節を引き、手先を逆方向の位置に移動させた状態。frame_${i + 1 === 0 ? "000" : i + 1 < 0 ? i + 1 : "+" + (i + 1)} と比べて、タメの深さが ${n} 段階分はっきり強く、手先位置も ${n} 段階分逆方向にずれている。胴体・足元は基準とほぼ一致（サイズ・カメラ固定）。`;
  }
  return `動作完了 ${i} ステップ後のフォロースルー。主動作部位（腕など）が基準姿勢から動作方向へさらに伸び、手先位置が動作方向へ大きく進んだ状態。frame_${i - 1 === 0 ? "000" : i - 1 < 0 ? i - 1 : "+" + (i - 1)} と比べて、振り抜き度合が ${i} 段階分はっきり強く、手先位置も ${i} 段階分動作方向にずれている。胴体・足元は基準とほぼ一致（サイズ・カメラ固定）。`;
}

function strengthGuide(s: "weak" | "medium" | "strong"): string {
  if (s === "weak") return "主動作部位の手先が各フレームで画面上 5〜10% 程度ずつ移動する。歪み変形だけは NG、関節を実際に動かす。";
  if (s === "strong") return "主動作部位の手先が各フレームで画面上 15〜25% 程度ずつ移動し、最終フレームでは基準から大きく離れた位置に来る。関節角度を大胆に変える。歪み変形だけにするのは絶対 NG。";
  return "主動作部位の手先が各フレームで画面上 10〜15% 程度ずつ移動する。関節角度を実際に変える。歪み変形だけは NG。";
}

function escape(s: string): string {
  return s.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\n/g, "\\n");
}
